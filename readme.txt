app.py、settings.pyが存在するフォルダをアプリと認識する。
アプリ名はフォルダ名を用いる。

app.pyを実行する関数は必ずrun()とする。
run()には実行結果をreturnさせるとよい。

gitにデプロイする際に、アクセストークン等外部からアクセスしてほしくない
変数をgitに残さないよう、.envファイルに環境変数を設定し、config.pyから読み込む。
herokuにデプロイする際には、herokuに環境変数をセットする。
